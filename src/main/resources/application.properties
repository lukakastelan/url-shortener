# Set the HTTP server port (adjust as needed)
quarkus.http.port=8080

# Set the host name or IP address (adjust as needed)
quarkus.http.host=localhost

# Set the base path for JAX-RS resources
quarkus.http.root-path=/
